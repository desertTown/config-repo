zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
      #zuul默认会过滤掉cookie相关信息， 如果现在其他服务中传递， 将这个属性设置为空即可
      sensitiveHeaders:
    #简洁写法
#    product: /myProduct/**
  # 禁止某些接口被外界直接访问
#  ignored-patterns:
#    - /**/product/list
